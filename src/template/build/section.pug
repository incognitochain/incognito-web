mixin section(subtitle, title, ...descs)
  .section&attributes(attributes)
    .info.container.visible-effect
      h3.subtitle= subtitle
      h1.title= title
      each desc in descs
        p.desc!= desc
    if block
      .content
        block