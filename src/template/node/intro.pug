mixin list(desc)
  li
    span= desc

mixin info-item(title, desc)
  .info-item
    .icon
      block
    .info
      span.title= title
      span.desc= desc

div#intro-container.bounceInUp-ani
  .content
    div.intro
      div
        h1.heading Incognito Node 
        h1.title Host a Node. Earn crypto.
        p.sub-title Node is a plug and play device that powers the Incognito network. Whenever anyone makes a private crypto transaction (say, in BTC), Node earns its owner rewards in that currency.
        p.sub-title Node also mines Incognito’s native coin, Privacy (PRV), for every block created. Earn pieces of the network for helping to build it.

        br
        .btn-groups
          a.btn.host-node(href='specs.html') Buy a Node
        br
        p.sub-title Prefer to do it yourself?  
          a(href='become-validator.html') Host a Node on your computer.

        //- ul
        //-   +list('Issue your own privacy token')
        //-   +list('Buy Privacy token')
        //-   +list('Mine and earn Privacy token')
        //-   +list('Partner with the Incognito blockchain')
        //-   +list('Read the whitepaper & open-source code')

      div.action-btn.scroll-action-btn(data-scroll-to='#section-container')
        img(src=require('../../image/icon/arrow_grey.svg'), alt='arrow icon')
        span Learn more
