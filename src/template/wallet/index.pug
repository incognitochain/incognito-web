extends ../layout.pug


block title
   title Send Bitcoin anonymously. Make your bitcoin untraceable.
   
block meta
   - var descriptionContent = `Looking to send Bitcoin anonymously? Now you can stay completely anonymous when you send Bitcoin, Ethereum or any other cryptocurrency. Use the Incognito anonymous bitcoin wallet to hide your bitcoin activity from prying eyes. Turn all your coins into privacy coins.`;
   meta(name='description' content=descriptionContent)
   meta(name='og:description' content=descriptionContent)
   meta(name='twitter:description' content=descriptionContent)

block main
   include ../header.pug
   //- include ../telegram_chat.pug
   div#wallet-container
      div.intro-slider-container
         .image
            .desktop
               i-slider(images=[{
                     img: require('../../image/wallet/wallet_slider_1.jpg'),
                     position: 'top',
                     paddingTop: 'calc(100vh - 60px)'
                  }
               ])
            img(src=require('../../image/wallet/wallet_mobile_bg.jpg')).mobile
         .right-content
            include intro.pug
      //- include section.pug
      //- include question.pug